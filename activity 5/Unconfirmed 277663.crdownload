def count_char_occurrences(s, char):
    return s.count(char)
input_string = "hello"
character_to_count = 'l'

count = count_char_occurrences(input_string, character_to_count)
print("Count:", count)
